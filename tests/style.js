for (let i = 0; i < 20; i++) {
    // Foo
    return __REDUX_DEVTOOLS_EXTENSION_COMPOSE__;
}

console.warn('Oh noes');
console.error('Oh noes');

const style = process.env.foo;
const USER_SHOW = id => `users/${id}`;
export default style;

const foo = 'bar';
switch (foo) {
    case 'bar':
        const baz = USER_SHOW(1);

        return baz;

    default:
        return 'bar';
}
